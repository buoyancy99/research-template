defaults:
  - base_experiment

# override corresponding fields in base_experiment
training:
  batch_size: 32
  max_epochs: 10
  checkpointing:
    every_n_train_steps: 10000
validation:
  check_interval: 1.0
  check_epoch: 1
  batch_size: 32
